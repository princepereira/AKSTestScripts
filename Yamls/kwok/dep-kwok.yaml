apiVersion: apps/v1
kind: Deployment
metadata:
  name: dep-kwok
  namespace: demo
  labels:
    app: server
spec:
  replicas: 1
  selector:
    matchLabels:
      app: server
  template:
    metadata:
      labels:
        app: server
    spec:
      nodeSelector:
        type: kwok
      tolerations:
        - key: "kwok.x-k8s.io/node"
          operator: "Exists"
      containers:
        - name: kwok-container
          image: mcr.microsoft.com/dotnet/framework/samples:aspnetapp
          ports:
            - containerPort: 80